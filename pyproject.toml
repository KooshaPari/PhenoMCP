[project]
name = "pheno-mcp"
version = "0.1.0"
requires-python = ">=3.14"
license = "MIT OR Apache-2.0"

[tool.mypy]
python_version = "3.14"
strict = false
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "tests.*"
ignore_errors = true